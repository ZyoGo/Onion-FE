import * as quiz from './quiz.js';

document.addEventListener('DOMContentLoaded', function () {
  //   quiz.createComponentQuestion();
  //   quiz.showQuestion(quiz.shuffledQuestions[quiz.currentQuestionIndex]);
  //   quiz.buttonNext.classList.add('hidden');
  quiz.hiddenButtonNavigation();
  //   console.log(quiz.showQuestion);
  //   quiz.showQuestion();
});
